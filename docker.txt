# get the docker image
wget -O honeypot.tar.zip https://tubcloud.tu-berlin.de/s/nWpkyFranBAACCa/download/honeypot.tar.zip

# unzip the image
unzip honeypot.tar.zip

# load the image
sudo docker load --input honeypot.tar



# commit changes to an image
# sudo docker commit [CONTAINER_ID] [new_image_name]
# sudo docker commit 0d3721b048cc honeypot_x86

# store a docker image
docker save --output honeypot.tar honeypot_x86

# load a docker image
sudo docker load --input honeypot.tar

# start a docker container and demonize
sudo docker run -d -t honeypot_x86:latest

# login to container
sudo docker exec -it [CONTAINER_ID] /bin/bash
sudo docker exec -it 47cbea7b9bfb /bin/bash


